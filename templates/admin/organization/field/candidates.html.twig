{% extends '@SonataAdmin/CRUD/base_show_field.html.twig' %}

{% block field %}
    {% for candidate in value %}
        <p><a href="{{ path('user_show', {'id': candidate.getId()}) }}">{{ candidate.getEmail() }}</a></p>
    {% endfor %}
{% endblock %}
